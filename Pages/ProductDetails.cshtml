@page
@model ECommerceWeb.Pages.ProductDetailsModel
@{
    ViewBag.Title = "ProductDetails";
}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
        }

        .product-main-image img {
            width: 100%;
            border: 2px solid #999966;
            border-radius: 10px;
            padding: 10px;
            background-color: #f8f9fa;
            box-shadow: 0 8px 16px rgba(255, 112, 67, 0.2);
            transition: transform 0.5s ease-in-out;
            cursor: pointer;
        }
        .product-main-image img:hover{
            transform:scale(4.05);
            border: 2px solid #704545;
        }
        .product-main-image img:hover {
            transform: scale(1.05);
        }

        .product-other-images img {
            width: 60px;
            height: 60px;
            margin: 5px;
            border-radius: 4px;
            cursor: pointer;
            transition: transform 0.3s;
            border: 2px solid #d670d6;
        }

        .product-other-images img:hover {
            transform:scale(4.5);
             border: 2px solid #aba663;
             box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .price-availability-block {
            margin-top: 20px;
            font-size: 1.2em;
        }

        .price {
            font-size: 1.7em;
            color: #c0392b;
            margin-bottom: 10px;
        }

        .availability {
            font-size: 1.3em;
            color: #27AE60;
        }

        .product-page-cart .btn {
            margin-right: 10px;
            transition: background-color 0.3s, color 0.3s;
        }

        /* .product-page-cart .btn:hover {
            background-color: #ff6347;
            color: white;
        } */

        .hidden {
            display: none;
        }

        #more-details-btn:hover {
            background-color: #f39c12;
            color: white;
        }

        /* .product-page-cart .btn-outline-secondary {
            background-color: #660066;
            color: #e085ff;
            
        } */

        input#product-quantity.form-control {
            text-align:center; 
            align-content:center;
            color:darkgreen;
            background-color: #ffebff;
            
            
        }

        lable.control-label {
            color: #7a3336;
        }


    /*-----------Product Size and Product Colors------*/


        .product-page-options label.control-label {
            font-weight: bold;
            color: #2c3e50;
          
        }

        .product-page-options select.form-control:focus {
            background-color: #ffffff; /* White background on focus */
            border-color: #ff3352; /* Light blue border on focus */
            box-shadow: 0px 0px 5px 0px rgba(52, 152, 219, 0.5);
        }

        .product-page-options select.form-control option {
            color: #2c3e50;
            font-size: 14px;
        }


        .form-group label.control-label {
            font-size:11px;
            color: #ad3300;
        }

        .form-group select.form-control {
            background-color: #f7f1ff;
            border: 2px solid #73a300;
            color: #34495e;
            transition: all 0.3s ease;
        }

            .form-group select.form-control:focus {  
                background-color: #ffffff;
                border-color: #d95200;
                box-shadow: 0px 0px 5px 0px rgba(155, 89, 182, 0.5);
            }

            .form-group select.form-control option {
                color: #34495e;
               
            }

       /*  #input.product-quantity.form-control{
            align-content:center;
            box-shadow: 0px 0px 5px 0px rgba(153, 89, 182, 0.8);
        } */

        <!---------------Buttons for increasing and decreasing---------------------->



        .quantity-input {
            display: flex;
            max-width: 180px;
            margin: 0 auto;
        }

        .btn-number {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px; /* Adjust font size */
            line-height: 0; /* Align icons vertically */
            padding: 0;

        }

        /* #product-quantity {
            width: 60px;
            text-align: center;
            border-left: 60px;
            border-right: 0;
        } */

      

            #product-quantity:focus {
                border-color: #0056b3; /* Change the border color when the input is focused */
                outline: none; /* Remove the default focus outline */
                box-shadow: 0 0 5px rgba(0, 91, 187, 0.5); /* Add a subtle shadow effect */
            }
        .btn-number:focus {
            outline: none;
            box-shadow: none;
        }

        .glyphicon {
            font-size: 16px;
            box-shadow: red;
            background-color: mediumseagreen;
        }

        .form-group label {
            font-weight: bold;
            margin-bottom: 10px;
            display: block;
        }

        .btn.btn-outline-secondary.btn-number{
            overflow:hidden;
            background-color: crimson;
            color:lightsteelblue;
        }

        input#product-quantity.form-control{
            overflow:hidden;
            /* display:flex; */
            background-color:cadetblue;
            color:darkred;
            border-left-color:blue;
            border-block-color:purple;
            justify-content:center;
            text-align:center;
          
            
            
        }
        button.btn.btn-outline-secondary.btn-number{
            align-items:center;
            overflow:hidden;
        }
      
        
       
        
/* 
        input:not([type="image" i], [type="range" i], [type="checkbox" i], [type="radio" i]) {
            overflow-clip-margin: 0px !important;
            overflow: clip !important;
        }

        user
        button, input, optgroup, select, textarea {
            margin: 0;
            font: inherit;
            color: inherit;
        } */


    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-4">
                <div class="product-main-image"> 
                    <img src="@Model.product.ProductDisplayImage" alt="Product Image" class="img-responsive">
                </div>
                <div class="product-other-images d-flex flex-wrap">
                    @foreach (var item in Model.productImages)
                    {
                        <a href=""><img alt="Additional Product Image" src="@item.ImageUrl"></a>
                       
                    }
                </div>
            </div>
            <div class="col-md-8">
                <h1>@Model.product.ProductName</h1>
                <div class="price-availability-block">
                    <div class="price">
                        <label>Price:</label>
                        <strong><span>@Model.product.Price</span></strong>
                    </div>
                    <div class="availability">
                        Availability: <strong>In Stock</strong>
                    </div>
                </div>
                <div class="description">
                    <p>@Model.product.ShortDescription</p>
                </div>
                <div class="description hidden" id="long-description">
                    <p>@Model.product.LongDescription</p>
                </div>
                <div class="product-page-cart">
                    <div class="form-group">
                        <label>Quantity</label>
                        <div class="input-group">
                            <div class="input-group-prepend">
                               <div class="col-md-2">
                                    <button type="button" class="btn btn-outline-secondary btn-number" data-type="minus" data-field="product-quantity">
                                    <span class="glyphicon glyphicon-minus"></span>
                                </button>
                            </div>


                                <input id="product-quantity" type="text" name="product-quantity" class="form-control" value="1" min="1" readonly >
                            <div class="input-group-append">
                                <button type="button" class="btn btn-outline-secondary btn-number" data-type="plus" data-field="product-quantity">
                                    <span class="glyphicon glyphicon-plus"></span>
                                </button>
                            </div>
                        </div>
                        </div>
                    </div>
                    <!--------------Product Size------------>
                    <div class="product-page-options row">
                        <div class="form-group col-md-6 col-sm-6">
                            <label class="control-label">Product Size</label>
                            <select class="form-control input-sm">
                                @foreach (var item in Model.productSize)
                                {
                                    <option value="@item.Name">@item.Name</option>
                                }
                            </select>
                        </div>
                        <!--------------Product Color------------>
                        <div class="form-group col-md-6 col-sm-6">
                            <label class="control-label">Product Color</label>
                            <select class="form-control input-sm">
                                @foreach (var item in Model.productColor)
                                {
                                    <option value="@item.Name">@item.Name</option>
                                }
                            </select>
                        </div>
                    </div> 
                   
                    <button class="btn btn-primary" type="submit">Add to cart</button>
                    <button id="more-details-btn" class="btn btn-secondary">More details</button>
                   
                </div>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            $('.btn-number').click(function (e) {
                e.preventDefault();
                var fieldName = $(this).attr('data-field');
                var type = $(this).attr('data-type');
                var input = $("input[name='" + fieldName + "']");
                var currentVal = parseInt(input.val());

                if (!isNaN(currentVal)) {
                    if (type === 'minus' && currentVal > input.attr('min')) {
                        input.val(currentVal - 1).change();
                    } else if (type === 'plus') {
                        input.val(currentVal + 1).change();
                    }
                } else {
                    input.val(1);
                }
            });

            $('#more-details-btn').click(function (e) {
                e.preventDefault();
                $('#long-description').slideToggle();
            });
        });
    </script>
</body>

</html>












@* <html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .product-main-image img {
            width: 100%;
            border: 1px solid #ddd;
            border-radius: 4px;
            /* padding: 5px */;
            padding: 10px;
            background-color: #e0e0d1; /* Light orange background */
            border: 2px solid #999966; /* Orange border */
            border-radius: 10px;
            box-shadow: 0 8px 16px rgba(255, 112, 67, 0.2);
            transition: transform 0.5s ease-in-out;
            cursor: pointer;
            width: 400px;
             /* height: 400px; */ 
            
        }

        .product-other-images img {
             width: 50px;
            height: 50px;
            margin: 5px;
            border: 1px solid #ddd;
            border-radius: 4px;
            cursor: pointer;
            transition: transform 0.2s; 

          
        }

        .product-main-image img:hover {
                background-color:darkviolet; 
        }

        
        .product-other-images img:hover {
            transform: scale(1.1);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transform: scale(4.05); /* Slightly enlarges the box */
            color: palevioletred;
        }

        .price-availability-block {
            margin-top: 20px;
            font-size: 1.2em;
        }

        .price {
            font-size: 1.5em;
            color: firebrick;
            margin-bottom: 10px;
        }

        .availability {
            font-size: 1.2em;
            color: #27AE60;
        }
        .availability {
            font-size: 1.2em;
            color: #27AE60;
        }

        .description {
            margin: 20px 0;
        }

        .product-page-options {
            margin: 20px 0;
        }

        .product-page-cart {
            margin: 20px 0;
        }

        .product-page-cart .btn {
            margin-right: 10px;
            transition: background-color 0.2s, color 0.2s;
        }

        .product-page-cart .btn:hover {
            background-color:brown;
            color: white;
        }

          .form-control.input-sm {
            max-width: 300px;
        }

        #product-quantity {
            width: 50px;
            text-align: center;
            transition: border-color 0.2s;
        }

        #product-quantity:hover {
            border-color: #000;
        }

        .button:hover {
            border:orange;

         
        }

         .price{
            font-size:15px;
            color:firebrick;
        }

       
        .btn-default.btn-number {

            background-color:lightblue;;
            color:white;
        }

       
        
        /* .productColor.btn:hover {
            background-color:darkblue;
            color:white;
        }

        .productSize.btn:hover {
            color:darkblue;
            color: white;
        } */

        button.btn-default.btn-number{
            background-color:olive;
        }
       button.btn-default.btn-number:hover{
            background-color:darkorange;
       }

    </style>

</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-sm-4 col-xs-12">
                <div class="product-main-image">

                    <!-- Model.product.ProductDisplayImage for Product details Image -->
                    <img src="@Model.product.ProductDisplayImage" alt="Product Image" class="img-responsive">
                </div>
                <div class="product-other-images">
                    @foreach (var item in Model.productImages)
                    {
                        <a href="JavaScript:;" class="active"><img alt="Additional Product Image" src="@item.ImageUrl"></a>
                    }
                </div>
            </div>
            <div class="col-md-8 col-sm-8 col-xs-12">
                <h1>@Model.product.ProductName</h1>
                <!--------Price------------>
                <div class="price-availability-block clear fix">
                    <div class="price">
                        <label class="control-label">Price:</label>
                        <strong><span>@Model.product.Price</span></strong>
                    </div>
                    <div class="availability">
                        Availability: <strong>In Stock</strong>
                    </div>
                </div>
                <!--------------ShortDescription-------->
                <div class="description">
                    <p>@Model.product.ShortDescription</p>
                </div>
                <!--------------LongDescription---------->
                <div class="description">
                    <p>@Model.product.LongDescription</p>
                </div>
                <!--------------Product Size------------>
                <div class="product-page-options row">
                    <div class="form-group col-md-6 col-sm-6">
                        <label class="control-label">Product Size</label>
                        <select class="form-control input-sm">
                            @foreach (var item in Model.productSize)
                            {
                                <option value="@item.Name">@item.Name</option>
                            }
                        </select>
                    </div>
                    <!--------------Product Color------------>
                    <div class="form-group col-md-6 col-sm-6">
                        <label class="control-label">Product Color</label>
                        <select class="form-control input-sm">
                            @foreach (var item in Model.productColor)
                            {
                                <option value="@item.Name">@item.Name</option>
                            }
                        </select>
                    </div>
                </div>

                <!--------------Quantity------------>
                <div class="product-page-cart">
                    <div class="form-group">
                        <label class="control-label">Quantity</label>

                        <!-- Quantity Input Group -->
                        <div class="input-group">
                            <span class="input-group-btn">
                                <button type="button" class="btn btn-default btn-number" data-type="minus" data-field="product-quantity">
                                    <span class="glyphicon glyphicon-minus"></span>
                                </button>
                            </span>
                            <input id="product-quantity" type="text" name="product-quantity" class="form-control input-number" value="1" min="1" readonly>
                            <span class="input-group-btn">
                                <button type="button" class="btn btn-default btn-number" data-type="plus" data-field="product-quantity">
                                    <span class="glyphicon glyphicon-plus"></span>
                                </button>
                            </span>
                        </div>

                        <p>@Model.product.Quality</p>
                    </div>

                    <button class="btn btn-primary" type="submit">Add to cart</button>
                    <button id="more-details-btn" class="btn btn-default">More details</button>


                    <!-- Hidden Long Description -->
                    <div id="long-description" class="hidden">
                        <p>@Model.product.LongDescription</p>
                    </div>
                </div>

            </div>
        </div>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            // Quantity button functionality
            $('.btn-number').click(function (e) {
                e.preventDefault();

                var fieldName = $(this).attr('data-field');
                var type = $(this).attr('data-type');
                var input = $("input[name='" + fieldName + "']");
                var currentVal = parseInt(input.val());

                if (!isNaN(currentVal)) {
                    if (type == 'minus') {
                        if (currentVal > input.attr('min')) {
                            input.val(currentVal - 1).change();
                        }
                        if (parseInt(input.val()) == input.attr('min')) {
                            $(this).attr('disabled', true);
                        }

                    } else if (type == 'plus') {
                        input.val(currentVal + 1).change();
                    }
                } else {
                    input.val(1);
                }
            });

            // More details button functionality
            $('#more-details-btn').click(function (e) {
                e.preventDefault();
                $('#long-description').toggleClass('hidden');
            });
        });
    </script>





</body>
</html> *@




























@*
<style>
    /*  .description p {
            font-size: 8px; /* Make the text smaller */
            color: brown; /* Change text color to a shade of blue */
            font-family: 'Arial', sans-serif; /* Apply a font family */
            font-weight: bold; /* Make the text bold */
            text-shadow: 1px 1px 2px #000; /* Add a text shadow */
        } */
</style>
<div class="row">
   <div class="col-md-4 col-sm-4 col-xs-3">
        <div class="product-main-image">

 <!--Model.product.ProductDisplayImage for Product details Image -->
     <img src="@Model.product.ProductDisplayImage" alt="Cool green dress with red bell" style="width:300px" class="img-responsive">
      </div>
        <div class="product-other-images">
            @foreach (var item in Model.productImages)
            {
                <a href="javascript:;" class="active"><img alt="Berry Lace Dress" src="@item.ImageUrl"></a>
            }
        </div>
   </div>
                      <!--------Price------------>
   <div class="col-md-8 col-sm-8 col-xs-9">
        <h1>@Model.product.ProductName</h1>
        <div class="price-availability-block clearfix">
          <div class="price">
                <label class="control-label">Price</label>
                <strong><span>@Model.product.Price</span></strong>
                @* <em>$<span>62.00</span></em> *@
         @*  </div>
          <div class="availability">
                Availability: <strong>In Stock</strong>
          </div>
        </div>
                 <!--------------ShortDescription-------->
         <div class="description">
            <p>
                @Model.product.ShortDescription
            </p>
         </div>
              <!--------------Product Size------------>
         <div class="product-page-options">
               <div class="pull-left">
                    <label class="control-label">Product Size</label>
                    <select class="form-control input-sm">
                        @foreach (var item in Model.productsize)
                        {
                            <option value="@item.Name">@item.Name</option>
                        }
                    </select>
               </div>
         </div>
       <div class="pull-left">
         <label class="control-label">Product Color</label>
          <select class="form-control input-sm">
           @foreach (var item in Model.productcolor)
           {
           <option value="@item.Name">@item.Name</option>
           }
          </select>
       </div>
   </div>

    <!------------------LongDescription---------------------->

        <div class="product-page-cart">
            <div class="product-quantity">
                <input id="product-quantity" type="text" value="1" readonly name="product-quantity" class="form-control input-sm">
            </div>
           <button class="btn btn-primary" type="submit">Add to cart</button>
          <a href="@Model.product.LongDescription" class="btn btn-default"> More details </a>
        </div>
     

    <div class="sticker sticker-sale">

    </div>

</div>
 *@








 
